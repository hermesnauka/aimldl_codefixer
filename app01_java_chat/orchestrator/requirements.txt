# Pinned runtime dependencies. Mirrors [project.dependencies] in pyproject.toml.
# Kept alongside pyproject.toml for Dockerfile simplicity (pip install -r requirements.txt
# without an editable/local-package install step).
#
# NOTE on versions: this dev environment only has Python 3.14 available (no 3.11/3.12
# interpreter to target instead). The task's suggested pins (fastapi 0.115.x, langchain
# 0.3.x, pydantic 2.10.x) predate Python 3.14 wheel support for pydantic-core (PyO3 0.22.x
# hard-refuses to build against a "too new" CPython, and there is no prebuilt wheel for
# cp314 at those old versions) — installing them fails at build time, not just a warning.
# Bumped to the latest versions that DO ship cp314 wheels and resolve cleanly together
# (this pulled in LangChain/LangGraph 1.x, a real major-version jump from the 0.3.x/0.2.x
# the task suggested). The Dockerfile targets python:3.12-slim instead, where the original
# older pins would also work fine — these bumped pins were only necessary to make `pytest`
# runnable in *this* 3.14-only host. If you re-pin to older versions for the Docker image,
# re-verify langchain's API surface (ChatOpenAI, astream, AIMessageChunk) still matches
# app/llm/*.py, since 0.3.x -> 1.x did change some import paths.
fastapi==0.139.0
uvicorn[standard]==0.51.0
langgraph==1.2.9
langchain==1.3.13
langchain-openai==1.3.5
psycopg[binary,pool]==3.2.10
httpx==0.28.1
python-dotenv==1.2.2
pydantic==2.13.4
